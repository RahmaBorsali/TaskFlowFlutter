{
  "name": "taskflow-backend",
  "version": "1.0.0",
  "description": "Backend API for TaskFlow using json-server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
